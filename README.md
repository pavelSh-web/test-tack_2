Gulp starter build (bower/scss) with image optimization.
=====================
To add this build to your project run the command:

`git clone https://github.com/pavelSh-web/gulp-start.git ./ && del /s .gitkeep && rd /s /q .git && npm i`
